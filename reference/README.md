# reference/

Local-only inputs (gitignored): `head-scan.glb` (photogrammetry scan of Jurek, 2026-09-11), `head-aligned.blend`, `head-mm.blend`, `head-mm.stl` (aligned: origin between the pupils on the cornea plane, X right, Y forward, Z up, mm; scaled to PD = 62 provisionally), `landmarks-mm.json`.

Regenerate from the GLB with the scripts in the session scratchpad (scan_align.py, scan_refine.py) — to be moved into `tools/` when stable.
